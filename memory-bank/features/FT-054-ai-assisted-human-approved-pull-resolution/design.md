---
title: "FT-054: Design"
doc_kind: feature
doc_function: canonical
purpose: "Selected solution and safety contract for FT-054 reviewable pull resolution plans."
derived_from:
  - brief.md
status: active
audience: humans_and_agents
must_not_define:
  - ft_054_scope
  - ft_054_acceptance_criteria
  - ft_054_evidence_contract
  - implementation_sequence
---

# FT-054: Design

## Design Pack

| Artifact | Role | Owns |
| --- | --- | --- |
| `design.md` | Feature-local solution owner | `SOL-*`, `C4-*`, `CTR-*`, `INV-*`, `FM-*`, `RB-*` |

## Context

The feature adds a public CLI plan/apply contract at the boundary between a human reviewer, an optional AI proposal, the local downstream payload, its ownership lock and a pinned source. `brief.md` requires exact review identity and atomic application; current ownership planning and transaction code provide the local baseline.

## C4 Applicability

| C4 ID | Decision | Trigger / reason | Artifact |
| --- | --- | --- | --- |
| `C4-01` | `C1` | The public CLI adds a human/agent interaction and a reviewable file contract across the source/downstream trust boundary. | Compact context map below |

### C4 Artifact

| Element | Interaction / boundary |
| --- | --- |
| Human reviewer | Reviews the canonical plan digest and records every ambiguous adapted action by creating a detached approval attestation with an authorized human-controlled signing key. |
| AI agent / UI | Reads plan/context and may write a candidate plan; has no approval channel or access to an authorized signing key. |
| `memory-bank-cli pull` | Emits a read-only JSON plan; verifies a detached approval attestation, validates the plan and atomically applies deterministic bytes. |
| Downstream payload + `memory-bank/.lock` | Local state to be identity-checked and committed together. |
| Pinned template source | Upstream payload and immutable source identity to be identity-checked. |

## Architecture Coverage Decision

| Aspect | Status | Canonical owner / refs | Supporting view / artifact | Coverage note |
| --- | --- | --- | --- | --- |
| Components / responsibilities | covered | `SOL-01`–`SOL-04` | C1 table | CLI planner, plan codec/validator and existing ownership transaction have distinct roles. |
| Connectors / interactions | covered | `CTR-01`–`CTR-02` | none | JSON file and local file/lock reads are explicit connectors. |
| Configuration / topology | covered | `SOL-01`, `CTR-01` | C1 table | Same local CLI topology; plan binds selected source and downstream paths. |
| Behavioral semantics | covered | `SOL-02`–`SOL-04`, `INV-*`, `FM-*` | none | Read-only planning, human decision, revalidation and atomic commit are specified. |
| Quality / evolution concerns | covered | `brief.md` `CON-*`; `INV-*`, `FM-*`, `RB-01` | none | Versioned schema, tamper resistance, compatibility and rollback are explicit. |

## Selected Solution

- `SOL-01` Add a versioned JSON resolution-plan representation generated by a non-mutating `pull` planning mode. Its canonical serialization records plan schema/version, pinned source identity, lock identity, and per-path ownership, base/local/upstream identities, proposed safe action, reason, allowed actions and decision state. The canonical whole-plan digest covers every action and result field.
- `SOL-02` Apply accepts a plan only with independently verifiable, tamper-resistant human authorization over its canonical whole-plan digest. `BD-01` must select and approve the attestation/signature format, canonicalization procedure, trust-store configuration, key lifecycle and reviewer signing workflow before this mechanism is implemented or the package is called Plan Ready. The selected mechanism must keep its signing authority outside the agent/plan-writer authority and cause the CLI to reject missing, invalid, unauthorized or mismatched authorization before staging.
- `SOL-03` For a proposed non-overlapping mechanical merge, planning records canonical base bytes whose digest must equal the recorded lock base identity, plus the local and upstream bytes/identities, merge algorithm version and result bytes/digest/mode. The CLI performs that specified deterministic three-way merge during planning, rejects an overlapping change, and recomputes it during apply; apply accepts the action only when its recomputed result and mode equal the reviewed, human-authorized fields. Other explicit human actions may select the validated local or upstream bytes, but cannot label arbitrary bytes as a mechanical merge.
- `SOL-04` Apply mode parses strictly, rejects unknown schema/action/path data, verifies human authorization, and derives final ownership changes only from valid accepted actions. It acquires an exclusive update guard covering the downstream payload, source checkout and ownership lock, then recomputes each recorded identity against current lock, source and local state immediately before staging and holds that guard through transaction commit. It rejects unresolved two-sided adapted paths; the ownership transaction stages payload and `.lock` together. Default `pull` remains its existing conservative behavior; plan/apply is opt-in and does not contact an LLM.

### Adapted one-sided state/action contract

| Recorded ownership | Local state relative to base | Upstream state relative to base | Action | Result ownership | Result content and mode | Human decision |
| --- | --- | --- | --- | --- | --- | --- |
| `adapted` | changed | unchanged | `keep-local` | `adapted` | Recorded local bytes and executable mode | Not required; deterministic safe action. |
| `adapted` | unchanged | changed | `take-upstream` | `adapted` | Recorded upstream bytes and executable mode | Not required; deterministic safe action. |
| `adapted` | changed | changed | unresolved | unchanged until a permitted action is human-authorized | No payload or mode change during plan/apply rejection | Required: `keep-local`, `take-upstream`, or `apply-reviewed-merge`. |

## Alternatives Considered

| Alternative ID | Option | Why not selected |
| --- | --- | --- |
| `ALT-01` | Let an AI or plan generator choose adapted actions. | Violates `REQ-04` and `CON-02`; a proposal is not human approval. |
| `ALT-02` | Re-run current planning and apply only path/action names. | Does not bind review to source, lock or exact payload/merge bytes and cannot detect all stale review inputs. |
| `ALT-03` | Retain terminal-only `--ask`. | Cannot be committed, reviewed asynchronously, or prepared by an agent. |

## Trade-offs

| Trade-off ID | Decision | Benefit | Cost / Risk |
| --- | --- | --- | --- |
| `TRD-01` | Require plan completeness, detached human approval and identity revalidation before every apply. | Prevents an agent-authored plan, stale inputs or a partly approved update from authorizing mutation. | Reviewers need an authorized signing key; a plan may need regeneration and re-approval after any relevant change. |
| `TRD-02` | Store and recompute the reviewed mechanical-merge result. | Human reviews the exact bytes/mode the CLI writes and the CLI proves they are the specified non-overlapping mechanical merge. | Larger plan files and a versioned deterministic merge implementation. |

## Accepted Local Decisions

- `SD-01` JSON is the feature-local plan serialization because existing CLI reports and locks already use JSON; schema versioning and strict decoding make the review carrier deterministic.
- `SD-02` `keep-and-detach` is represented as a deterministic safe action only for the issue-defined missing-template user-owned case; it never licenses deletion of user-owned content.
- `SD-03` **Blocked by `BD-01`.** The concrete approval carrier, canonicalization procedure, trust-store configuration, key lifecycle and reviewer signing workflow require explicit human approval. Whichever mechanism is selected must satisfy `SOL-02` without granting an agent/plan writer approval authority.

## Contracts

| Contract ID | Connector / direction | Roles and sync boundary | Guarantees / failure / evolution semantics |
| --- | --- | --- | --- |
| `CTR-01` | Versioned JSON plan plus selected human-authorization carrier: CLI/agent -> reviewer -> CLI | Writer may be CLI or agent; the reviewer authorizes only through the `BD-01`-approved workflow; apply synchronously reads both carriers. | Strict schema/path validation and canonical digest; selected authorization covers every plan field. Unknown, missing, unauthorized, tampered or stale data fails before mutation; schema/merge-algorithm versions support explicit evolution. |
| `CTR-02` | Local source/lock/payload reads -> exclusive update guard -> ownership transaction | CLI is deterministic executor; no external AI provider; all compliant writers use the same guard. | Acquire the guard, perform final identity validation and completed-decision/mechanical-merge recomputation, then commit payload and lock before release. A competing compliant writer waits or fails without interleaving; transaction commits payload and lock together or leaves both unchanged. |

## Invariants

- `INV-01` Plan generation and every rejected apply leave downstream payload and `.lock` unchanged.
- `INV-02` Apply may mutate only when valid human authorization covers the exact canonical plan digest and, while holding the exclusive update guard, each plan-bound source, lock, local and upstream identity still matches that authorized plan.
- `INV-03` No unresolved two-sided adapted path, AI inference, unauthorized plan change or result not reproduced by the specified mechanical merge can authorize mutation.
- `INV-04` File content and executable mode are part of the result reviewed and applied for any action that replaces or merges a file.
- `INV-05` A plan is applicable only to its recorded pre-state. After a successful apply changes a bound identity, replaying that plan is stale and must be rejected; determinism is demonstrated by applying the same human-authorized plan to separate identical pre-state fixtures.
- `INV-06` A compliant writer cannot modify the guarded payload, source checkout or ownership lock between final identity validation and the transaction commit.

## Failure Modes

- `FM-01` Schema, action, path or digest tampering, or missing/invalid/unauthorized human authorization, is rejected during strict parse/validation before staging.
- `FM-02` Any stale lock/source/local/template payload is rejected before staging; the exclusive update guard prevents a compliant writer from creating a validation-to-commit race, and the reviewer regenerates and re-reviews a plan.
- `FM-03` A staging or commit failure invokes the existing transaction rollback, leaving payload and lock unchanged.

## Rollout / Backout

| Stage ID | Stage | Entry condition | Backout |
| --- | --- | --- | --- |
| `RB-01` | Release opt-in plan/apply alongside existing `pull` behavior. | `CHK-01`–`CHK-05` pass and docs state the approval boundary. | Users continue default conservative `pull`; no migration of existing locks or plans is required. |

## Design Verification

| Analysis | Required | Reason / risk | Method | Result / evidence |
| --- | --- | --- | --- | --- |
| Contract compatibility | yes | New CLI and versioned plan-file contract. | Strict codec/API and help review. | `CTR-01`; `CHK-01`, `CHK-05`. |
| State / transition completeness | yes | Plan has proposed, unresolved, accepted and rejected paths, plus a single applicable pre-state. | Scenario/state-table walk-through. | `SOL-01`–`SOL-04`, `INV-05`, `SC-01`–`SC-05`. |
| Failure propagation | yes | Failed validation/write must not partially mutate lock or files. | Tamper/stale/injected-failure E2E. | `FM-01`–`FM-03`; `CHK-04`. |
| Concurrency / ordering | yes | A synchronous apply can still race another writer after initial validation. | Exercise the exclusive update guard and transaction-scoped final revalidation with competing-writer E2E. | `SOL-04`, `CTR-02`, `INV-02`, `INV-06`; `CHK-04`. |
| Security boundaries | yes | Plan is untrusted input and an AI is explicitly untrusted for approval. | Strict-input and authority-boundary review. | `CON-01`–`CON-02`, `INV-02`–`INV-03`; `CHK-04`–`CHK-05`. |
| Capacity / latency | no | No remote or background path; plans contain only changed-path context. | N/A. | N/A. |
| Migration / evolution safety | yes | Existing pull and locks must remain compatible; plan, merge-algorithm and selected human-authorization formats will evolve. | Compatibility regression and opt-in rollout review. | `SOL-04`, `RB-01`; `CHK-02`, `CHK-05`. |
