---
title: "FT-054: Design"
doc_kind: feature
doc_function: canonical
purpose: "Selected solution and safety contract for FT-054 reviewable pull resolution plans."
derived_from:
  - brief.md
status: active
audience: humans_and_agents
must_not_define:
  - ft_054_scope
  - ft_054_acceptance_criteria
  - ft_054_evidence_contract
  - implementation_sequence
---

# FT-054: Design

## Design Pack

| Artifact | Role | Owns |
| --- | --- | --- |
| `design.md` | Feature-local solution owner | `SOL-*`, `C4-*`, `CTR-*`, `INV-*`, `FM-*`, `RB-*` |

## Context

The feature adds a public CLI plan/apply contract at the boundary between a human reviewer, an optional AI proposal, the local downstream payload, its ownership lock and a pinned source. `brief.md` requires exact review identity and atomic application; current ownership planning and transaction code provide the local baseline.

## C4 Applicability

| C4 ID | Decision | Trigger / reason | Artifact |
| --- | --- | --- | --- |
| `C4-01` | `C1` | The public CLI adds a human/agent interaction and a reviewable file contract across the source/downstream trust boundary. | Compact context map below |

### C4 Artifact

| Element | Interaction / boundary |
| --- | --- |
| Human reviewer | Reviews the canonical plan digest and records every ambiguous adapted action through the human-authorization workflow selected by `BD-01`. |
| AI agent / UI | Reads plan/context and may write a candidate plan; has no human-authorization authority or access to the human-controlled material required by the selected workflow. |
| `memory-bank-cli pull` | Emits a read-only JSON plan; verifies human authorization through the `BD-01`-selected workflow, validates the plan and atomically applies deterministic bytes. |
| Downstream payload + `memory-bank/.lock` | Local state, including any versioned lock-retained historical-base snapshot, to be identity-checked and committed together. |
| Pinned template source | Upstream payload and immutable source identity to be identity-checked. |

## Architecture Coverage Decision

| Aspect | Status | Canonical owner / refs | Supporting view / artifact | Coverage note |
| --- | --- | --- | --- | --- |
| Components / responsibilities | covered | `SOL-01`–`SOL-04` | C1 table | CLI planner, plan codec/validator and existing ownership transaction have distinct roles. |
| Connectors / interactions | covered | `CTR-01`–`CTR-02` | none | JSON file and local file/lock reads are explicit connectors. |
| Configuration / topology | covered | `SOL-01`, `CTR-01` | C1 table | Same local CLI topology; plan binds selected source and downstream paths. |
| Behavioral semantics | covered | `SOL-02`–`SOL-04`, `INV-*`, `FM-*` | none | Read-only planning, human decision, revalidation and atomic commit are specified. |
| Quality / evolution concerns | covered | `brief.md` `CON-*`; `INV-*`, `FM-*`, `RB-01` | none | Versioned schema, tamper resistance, compatibility and rollback are explicit. |

## Selected Solution

- `SOL-01` Add a versioned JSON resolution-plan representation generated by a non-mutating `pull` planning mode. Its canonical serialization records plan schema/version, pinned source identity, lock identity, and per-path ownership, base/local/upstream identities, proposed safe action, reason, allowed actions and decision state. The canonical whole-plan digest covers every action and result field.
- `SOL-02` Apply accepts a plan only with independently verifiable, tamper-resistant human authorization over its canonical whole-plan digest. `BD-01` must select and approve the human-authorization carrier and verification format, canonicalization procedure, trust policy/configuration, credential lifecycle and reviewer authorization workflow before this mechanism is implemented or the package is called Plan Ready. The selected mechanism must keep approval authority outside the agent/plan-writer authority and cause the CLI to reject missing, invalid, unauthorized or mismatched authorization before staging.
- `SOL-03` A non-overlapping mechanical merge is available only when the base bytes are recoverable from authoritative historical-base state retained with the ownership lock. For each newly established or accepted upstream base, the ownership transaction atomically retains the canonical base bytes and executable mode, with their digest, mode and path binding; planning verifies that retained state against the lock before recording it in a plan. It must never reconstruct a base from a digest, a plan-supplied value, or an unverified source checkout. For a proposed merge, planning records those verified base bytes, the local and upstream bytes/identities, the `mbc-diff3-lines-v1` algorithm identifier and the result bytes/digest/mode. The CLI runs that algorithm during planning and recomputes it during apply; apply accepts the action only when its result and mode equal the reviewed, human-authorized fields. A legacy lock entry without verifiable historical-base state marks `apply-reviewed-merge` unavailable; it may use another permitted human action, but it cannot be mechanically merged. A later clean baseline/update may establish retained historical-base state atomically; a digest alone cannot backfill it.
- `SOL-04` Apply mode parses strictly, rejects unknown schema/action/path data, verifies human authorization, and derives final ownership changes only from valid accepted actions. It acquires an exclusive update guard covering the downstream payload, source checkout and ownership lock, then recomputes each recorded identity against current lock, source and local state immediately before staging and holds that guard through transaction commit. The guard serializes only writers that participate in it; a non-cooperating writer is outside this mutual-exclusion guarantee, though any change observed by final revalidation rejects the apply before staging. It rejects unresolved two-sided adapted paths; the ownership transaction stages payload and `.lock` together. Default `pull` remains its existing conservative behavior; plan/apply is opt-in and does not contact an LLM.

### Mechanical-merge algorithm: `mbc-diff3-lines-v1`

`mbc-diff3-lines-v1` is the normative algorithm for `apply-reviewed-merge`; no platform `diff3` binary, library default or implementation-specific heuristic is part of the contract. It operates only on regular, NUL-free files. If any of the verified base, local or upstream byte streams contains a NUL byte, mechanical merge is unavailable. It treats each byte stream as complete lines ending in LF, preserving every byte in each line (including a preceding CR); a final unterminated byte sequence is one final line. It performs no character decoding, line-ending normalization or whitespace normalization.

For each side, derive base-to-side edit hunks from a longest common subsequence of those line sequences. When multiple longest common subsequences exist, select the one whose ordered sequence of `(base-line-index, side-line-index)` pairs is lexicographically smallest. A hunk replaces one contiguous half-open base-line interval with its side's replacement lines. The merge is unavailable when the two hunk sets collide: two non-empty base intervals intersect, two insertions use the same base boundary, or an insertion is strictly inside another hunk's non-empty base interval. Otherwise, emit the base lines in order: after a non-empty hunk ends, emit an insertion at that boundary, then begin a non-empty hunk at that boundary. The result is encoded by concatenating those preserved lines exactly.

The result mode is: the common local/upstream mode when they are equal; otherwise the changed side's mode when the other side equals the base mode. If both sides differ from the base mode and from each other, mechanical merge is unavailable as a mode conflict. Thus binary input, a content collision or a mode conflict has no merge result to encode, and apply rejects any plan that nevertheless selects `apply-reviewed-merge`.

### Adapted state/action contract

| Recorded ownership | Local state relative to base | Upstream state relative to base | Action | Payload after apply | Ownership / `.lock` after apply | Human decision |
| --- | --- | --- | --- | --- | --- | --- |
| `adapted` | changed | unchanged | `keep-local` | Recorded local bytes and executable mode. | Retain `adapted`; retain the current recorded upstream base identity/mode and its verified historical-base state. | Not required; deterministic safe action. |
| `adapted` | unchanged | changed | `take-upstream` | Recorded upstream bytes and executable mode. | Retain `adapted`; atomically replace the recorded upstream base identity/mode and retained historical-base state with the current upstream bytes and mode. | Not required; deterministic safe action. |
| `adapted` | changed | changed | unresolved | No payload or mode change; apply rejects the plan. | Existing ownership and lock state remain unchanged. | Required: `keep-local`, `take-upstream`, or `apply-reviewed-merge`. |
| `adapted` | changed | changed | human-authorized `keep-local` | Recorded local bytes and executable mode. | Retain `adapted`; atomically replace the recorded upstream base identity/mode and retained historical-base state with the current upstream bytes and mode. | Required and must be covered by valid human authorization. |
| `adapted` | changed | changed | human-authorized `take-upstream` | Recorded upstream bytes and executable mode. | Retain `adapted`; atomically replace the recorded upstream base identity/mode and retained historical-base state with the current upstream bytes and mode. | Required and must be covered by valid human authorization. |
| `adapted` | changed | changed | human-authorized `apply-reviewed-merge` | Exact bytes and mode output by `mbc-diff3-lines-v1`, equal to the reviewed plan result. | Retain `adapted`; atomically replace the recorded upstream base identity/mode and retained historical-base state with the current upstream bytes and mode, not the merge result. | Required and available only when `mbc-diff3-lines-v1` produces a result from the verified base, local and upstream inputs. |

### Deterministic safe state/action contract

| Recorded ownership and state | Action | Payload after apply | Ownership / `.lock` after apply | Human decision |
| --- | --- | --- | --- | --- |
| `managed`; local equals recorded base and upstream has changed | `take-upstream` | Write the recorded upstream bytes and executable mode. | Retain `managed`; atomically replace the path's lock entry with the resulting upstream identity and mode. | Not required; deterministic safe action. |
| `user-owned`; path is absent from the current template | `keep-and-detach` | Preserve the existing local bytes and executable mode exactly; no deletion or rewrite. | Remove the path's ownership entry from `.lock`; commit that lock update atomically with the unchanged payload. | Not required; deterministic safe action. |

## Alternatives Considered

| Alternative ID | Option | Why not selected |
| --- | --- | --- |
| `ALT-01` | Let an AI or plan generator choose adapted actions. | Violates `REQ-04` and `CON-02`; a proposal is not human approval. |
| `ALT-02` | Re-run current planning and apply only path/action names. | Does not bind review to source, lock or exact payload/merge bytes and cannot detect all stale review inputs. |
| `ALT-03` | Retain terminal-only `--ask`. | Cannot be committed, reviewed asynchronously, or prepared by an agent. |

## Trade-offs

| Trade-off ID | Decision | Benefit | Cost / Risk |
| --- | --- | --- | --- |
| `TRD-01` | Require plan completeness, independently verifiable human authorization and identity revalidation before every apply. | Prevents an agent-authored plan, stale inputs or a partly approved update from authorizing mutation. | Reviewers must use the `BD-01`-approved workflow; a plan may need regeneration and re-approval after any relevant change. |
| `TRD-02` | Store and recompute the reviewed mechanical-merge result. | Human reviews the exact bytes/mode the CLI writes and the CLI proves they are the specified non-overlapping mechanical merge. | Larger plan files and a versioned deterministic merge implementation. |

## Accepted Local Decisions

- `SD-01` JSON is the feature-local plan serialization because existing CLI reports and locks already use JSON; schema versioning and strict decoding make the review carrier deterministic.
- `SD-02` `keep-and-detach` is represented as a deterministic safe action only for the issue-defined missing-template user-owned case. Before apply, the path is `user-owned`, has a lock entry, and is absent from the current template; apply preserves its exact bytes and executable mode, removes its ownership entry from `.lock`, and atomically commits the unchanged payload with that lock change. It never licenses deletion or rewrite of user-owned content.
- `SD-03` **Blocked by `BD-01`.** The concrete approval carrier and verification format, canonicalization procedure, trust policy/configuration, credential lifecycle and reviewer authorization workflow require explicit human approval. Whichever mechanism is selected must satisfy `SOL-02` without granting an agent/plan writer approval authority.

## Contracts

| Contract ID | Connector / direction | Roles and sync boundary | Guarantees / failure / evolution semantics |
| --- | --- | --- | --- |
| `CTR-01` | Versioned JSON plan plus selected human-authorization carrier: CLI/agent -> reviewer -> CLI | Writer may be CLI or agent; the reviewer authorizes only through the `BD-01`-approved workflow; apply synchronously reads both carriers. | Strict schema/path validation and canonical digest; selected authorization covers every plan field. Unknown, missing, unauthorized, tampered or stale data fails before mutation; schema/merge-algorithm versions support explicit evolution. |
| `CTR-02` | Local source/lock/payload and lock-retained historical-base snapshot reads -> exclusive update guard -> ownership transaction | CLI is deterministic executor; no external AI provider; all cooperating writers use the same guard. | Acquire the guard, verify a mechanical-merge base snapshot against its lock binding when present, perform final identity validation and completed-decision/mechanical-merge recomputation, then commit payload and lock (including any newly established base snapshot) before release. A missing legacy snapshot makes mechanical merge unavailable rather than guessed. A competing cooperating writer waits or fails without interleaving; transaction commits all bound state together or leaves it unchanged. A non-cooperating writer is not mutually excluded; a mutation observed during final validation rejects before staging, while an unobservable commit-boundary race remains outside this contract. |

## Invariants

- `INV-01` Plan generation and every rejected apply leave downstream payload and `.lock` unchanged.
- `INV-02` Apply may mutate only when valid human authorization covers the exact canonical plan digest and, while holding the exclusive update guard, each plan-bound source, lock, local and upstream identity still matches that authorized plan.
- `INV-03` No unresolved two-sided adapted path, AI inference, unauthorized plan change or result not reproduced by the specified mechanical merge can authorize mutation.
- `INV-04` File content and executable mode are part of the result reviewed and applied for any action that replaces or merges a file.
- `INV-05` A plan is applicable only to its recorded pre-state. After a successful apply changes a bound identity, replaying that plan is stale and must be rejected; determinism is demonstrated by applying the same human-authorized plan to separate identical pre-state fixtures.
- `INV-06` A cooperating writer cannot modify the guarded payload, source checkout or ownership lock between final identity validation and the transaction commit. Non-cooperating processes are not covered by this invariant.
- `INV-07` A mechanical merge never uses a reconstructed or plan-supplied historical base: its bytes and mode must be recovered from, and verified against, the lock-retained historical-base snapshot. A legacy entry without that snapshot cannot select `apply-reviewed-merge`.

## Failure Modes

- `FM-01` Schema, action, path or digest tampering, or missing/invalid/unauthorized human authorization, is rejected during strict parse/validation before staging.
- `FM-02` Any stale lock/source/local/template payload observed during final validation is rejected before staging; the exclusive update guard prevents a cooperating writer from creating a validation-to-commit race, and the reviewer regenerates and re-reviews a plan. A non-cooperating mutation that is not observable by final validation is outside the serialization contract.
- `FM-03` A staging or commit failure invokes the existing transaction rollback, leaving payload and lock, including any historical-base snapshot, unchanged.

## Rollout / Backout

| Stage ID | Stage | Entry condition | Backout |
| --- | --- | --- | --- |
| `RB-01` | Release opt-in plan/apply alongside existing `pull` behavior. | `CHK-01`–`CHK-05` pass and docs state the approval boundary. | Users continue default conservative `pull`; no migration of existing locks or plans is required. |

## Design Verification

| Analysis | Required | Reason / risk | Method | Result / evidence |
| --- | --- | --- | --- | --- |
| Contract compatibility | yes | New CLI and versioned plan-file contract. | Strict codec/API and help review. | `CTR-01`; `CHK-01`, `CHK-05`. |
| State / transition completeness | yes | Plan has proposed, unresolved, accepted and rejected paths, plus a single applicable pre-state. | Scenario/state-table walk-through, including managed no-drift update, user-owned missing-template detach content/mode/ownership/lock outcomes, and legacy entries without a historical base. | `SOL-01`–`SOL-04`, `SD-02`, `INV-05`, `INV-07`, `SC-04c`–`SC-04e`, `CHK-03`. |
| Failure propagation | yes | Failed validation/write must not partially mutate lock or files. | Tamper/stale/injected-failure E2E. | `FM-01`–`FM-03`; `CHK-04`. |
| Concurrency / ordering | yes | A synchronous apply can still race another writer after initial validation. | Exercise the exclusive update guard and transaction-scoped final revalidation with cooperating-writer E2E; document non-cooperating commit-boundary races as outside the advisory guard's guarantee. | `SOL-04`, `CTR-02`, `INV-02`, `INV-06`; `CHK-04`. |
| Security boundaries | yes | Plan is untrusted input and an AI is explicitly untrusted for approval. | Strict-input and authority-boundary review. | `CON-01`–`CON-02`, `INV-02`–`INV-03`; `CHK-04`–`CHK-05`. |
| Capacity / latency | no | No remote or background path; plans contain only changed-path context. | N/A. | N/A. |
| Migration / evolution safety | yes | Existing pull and locks must remain compatible; plan, merge-algorithm and selected human-authorization formats will evolve. | Compatibility regression and opt-in rollout review, including legacy entries without retained base snapshots. | `SOL-03`–`SOL-04`, `RB-01`; `CHK-02`, `CHK-03`, `CHK-05`. |
